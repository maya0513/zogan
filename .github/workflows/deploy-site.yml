name: deploy-site

on:
  workflow_run:
    workflows: [ci]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

concurrency:
  group: production-introduction
  cancel-in-progress: false

jobs:
  introduction:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main')
    name: Introduction site
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: production-introduction
      url: ${{ steps.deploy.outputs.deployment-url }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: 24
          cache: true
      # setup-vp manages pnpm for `vp`, but wrangler-action invokes the
      # configured package manager directly and therefore needs it on PATH.
      - uses: pnpm/action-setup@v4
      - run: vp install --frozen-lockfile
      - run: vp run @zogan/site#build
      - name: Deploy Workers Static Assets
        id: deploy
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          packageManager: pnpm
          workingDirectory: examples/site
          wranglerVersion: "4.120.0"
          command: deploy
